<template>
  <div>
    <p>
      To add a feature, first make sure the page you're editing has a subfolder named <code>Feature</code>. 
      If not, right click on the current page in the content directory, and select <code>New > Folder</code>. 
      To add the new feature, right click on the <code>Feature</code> folder, and select <code>New > OHA Feature</code>,
      as shown below.
    </p>
    <img :src="$withBase('/screenshots/add-feature.png')" alt="Add a feature" style="max-width: 500px;">
    <p>
      Now that your new feature has been created, follow the steps in the <a href="#feature-fields">"Feature Fields"</a> section below
      for more information on configuring and editing the various feature fields.
    </p>
  </div>
</template>
